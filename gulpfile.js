//require gulp so gulp can run
var gulp = require('gulp');

//creating task that prints message to console to make sure everything's running a-ok
gulp.task('print', function(){
	console.log("Up and running so far!");
});